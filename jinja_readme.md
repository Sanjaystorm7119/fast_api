
jinja is a fast, expressive and extensible templating language.

It's able to write code similar to Python in the Dom, and the template passes data to render within the final document.

So what are Jinja templating tags and scripts?

Well, Jinja tags allow developers to be confident while working within back end data.

Using tags that are similar to HTML.

So for example, on our front end there might be a link that references a stylesheet.

But right here in the href we can see that it's going to say URL for static.

And then it says our path to where our CSS lives.

